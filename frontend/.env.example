# Public endpoint only. Never put database passwords or API secrets in VITE_*.
# Leave empty while preparing the portal; visits are not sent or fabricated.
# For a same-origin Python API behind a reverse proxy: /api/visits
VITE_VISITS_API_URL=
